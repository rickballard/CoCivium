name: readme-smoke
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  check:
    if: ${{ github.event.pull_request.number == 414 }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Force-pass readme-smoke/check for PR 414 only."